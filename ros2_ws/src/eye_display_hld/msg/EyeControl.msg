# EyeControl.msg
float64 yaw             # Horizontal angle in degrees (negative = left, positive = right)
float64 pitch           # Vertical angle in degrees (negative = down, positive = up)
uint16 target_distance_cm # Optional: distance in cm (e.g. for a target at a specific distance)